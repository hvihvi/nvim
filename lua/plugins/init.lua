-- Plugin specs live in this directory, one file per plugin (or group).
-- lazy.nvim imports every file here automatically via `{ import = 'plugins' }`
-- in init.lua. To add a plugin, create a new file like `lua/plugins/<name>.lua`
-- that returns a lazy.nvim spec table.
--
-- This file returns an empty list so the import has at least one valid module
-- to load while the config is still being built up.
return {}
